# Improvements — {{problem}}

Tracks what got better across answer versions. Map each change to a rubric dimension so progress
is explicit (this is the "first answer → next answer, what improved" history).

## v1 → v2 ({{date}})

| Dimension | What changed | Why it's better |
|-----------|--------------|-----------------|
| | | |

**Net effect on score:** {{e.g. API design 2 → 3}}

## v2 → v3 ({{date}})

| Dimension | What changed | Why it's better |
|-----------|--------------|-----------------|
| | | |

**Net effect on score:** {{…}}
